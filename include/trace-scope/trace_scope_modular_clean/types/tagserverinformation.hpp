#ifndef TAGSERVERINFORMATION_HPP
#define TAGSERVERINFORMATION_HPP

/**
 * @file tagserverinformation.hpp
 * @brief tagServerInformation struct definition
 */

if (handle.handle) {
            handle.mapped_view = MapViewOfFile(handle.handle, FILE_MAP_ALL_ACCESS, 0, 0, size);
            handle.valid = (handle.mapped_view != nullptr);
        }
#else
 

#endif // TAGSERVERINFORMATION_HPP
